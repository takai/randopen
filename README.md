# randopen

Open a file in the directory randomly.

## Usage

```
  -path string
    	Target directory (required)
  -suffix string
    	Target suffix (default "wmv")
```

## Example

```
$ randopen -path .
```